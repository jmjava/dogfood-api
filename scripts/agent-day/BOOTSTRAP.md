# New Cloud Agent

Read and run from `embabel-dif`:

```bash
./scripts/bootstrap-cursor-agent-day.sh
```

Story: `../../scripts/HANDOFF-CURSOR-AGENT-DAY.md` in embabel-dif
(or this repo’s sibling). Secret name: **`CORRECT_CURSOR_KEY`**.
